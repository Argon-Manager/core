type Mutation {
  createProject(input: ProjectInput): Project!
}

type Project {
  id: ID!
  name: String!
  description: String
  users: [User]
}

input ProjectInput {
  name: String!
  description: String
}